{
  "name": "push2talk",
  "version": "0.1.0",
  "private": true,
  "description": "Push 2 Talk - push-to-talk voice dictation. Electron shell, spawns the Python backend (dev: system Python; packaged: frozen PyInstaller executable).",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test tests/test_diagnostics.mjs tests/test_login_item.mjs tests/test_fatal_lifecycle.mjs tests/test_single_instance.mjs"
  },
  "devDependencies": {
    "electron": "43.2.0",
    "electron-builder": "26.15.3"
  }
}
